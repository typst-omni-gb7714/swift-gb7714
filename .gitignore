# ============================================================
# 白名单制：默认忽略仓库里的一切，仅显式放行已审定的条目。
# 新增要纳入版本管理的顶层条目时，往下面的放行清单里加 `!/<名字>`。
# ============================================================

# 1) 先忽略根目录下的所有东西
/*

# 2) 仅放行这些顶层条目
!/.gitignore
!/README.md
!/plugin/
!/examples/

# 3) 放行目录“内部”再挡掉构建/渲染产物与系统垃圾
/plugin/target/        # Rust 构建产物
*.pdf                  # Typst 渲染输出（用例可由 .typ 重新编译）
.DS_Store
*.swp
