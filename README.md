至202606290638时刻，这套架构的可行性已被论证，我准备继续攻下omni-gb7714，并争取在2025标准正式实施（2026-07-01）附近发布到universe。未来单独做一套完全干净的swift-gb7714（暂定名）实现，并作为omni-gb7714的快速分支合并进去。此后我打算用这套新架构逐渐替换原先的omni解析层，两库届时完成整合。

我不太喜欢公开不成熟的东西，因此这里只是简单说一下swift的大致思路，有同仁志士看见了也可以试着做一下：

```
wasm(算 + 著录)                     原生 #bibliography(交付)            薄 Typst 粘合层
─────────────────                  ──────────────────────            ────────────────
• 解析(citegeist/自研)              • 只列被引 ✓                       • AY 正文标注(注1)
• 每条成品文本(全部 A 类特性)        • 按注入键排序 ✓(刚证)             • back-ref/脚注引用
• 拼音/笔画排序键 ←auto-pinyin/bihua  • verbatim 透传 ✓                  • 单次 cite 覆盖
• 语种判定 ←glotter                 • 版式属性 ✓                    • sort-keys 手动序
• 2020a/b 消歧后缀(注2)            • LSP 补全(它就是 std.bib)✓
• /OL、PID、双语 related…
        │ 哨兵                              ▲ bytes(yaml) + 注入排序键/消歧后缀
        └──────────────────────────────────┘   show regex 还原富文本
```

我们需要写多套 `.csl` 给原生的bibliography去使用，以完美配合我们wasm的输出。
